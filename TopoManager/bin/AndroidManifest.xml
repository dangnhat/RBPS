<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dhbk.android.topomanager"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="dhbk.android.topomanager.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="dhbk.android.topomanager.DetailActivity"
            android:label="@string/title_activity_detail" >
        </activity>
        <activity
            android:name="dhbk.android.topomanager.MeasureActivity"
            android:label="@string/title_activity_measure" >
        </activity>
        <activity
            android:name="dhbk.android.topomanager.PredictActivity"
            android:label="@string/title_activity_predict" >
        </activity>
        <activity
            android:name="dhbk.android.topomanager.ScheduleActivity"
            android:label="@string/title_activity_schedule" >
        </activity>
    </application>

</manifest>
